# 明日方舟 · 剧情图谱与时间线工具 — 版本 v1.1

> 发布日期：2026-08-28
> 上一版本：v1.0（2026-08-23）
> 数据源：PRTS Wiki 剧情数据 + md 人物档案核对（截至 2026-08）

## 版本定位

在 v1.0 双视图工具基础上，完成 **头像/阵营图标本地化**、**数据补全与档案核对**、**图谱视觉与交互全面增强** 的 v1.1 正式版。下载本目录全部文件即可离线运行（本地 http 服务打开 index.html，体验最佳）。

## 相比 v1.0 的主要变化

### 数据层
- `graph.json`：节点 956 → **982**（新增 26 个规范点名干员：陈/煌/年/夕/令/W/Logos/山 等，修正 12F 误判），重要角色 111 → **129**
- 边 10724 → **10942**（依据 md 人物档案核对补 173 条真实关系）
- 隐藏 601 个非关键/群演节点（全量视图过滤 `is_extra` 无名群演）
- 新增 `relation_details.json`（6.5MB）：全部边的档案互提 + 共同出场幕（511 对档案关联 / 9127 对共同经历）
- 时间线主线章节加 EP00~EP17 序号（273 段），按用户 EP 分组
- 新增 `faction_logos.json`（32 阵营图标映射）、`avatars.json`（头像映射）

### 头像与图标本地化
- 283 个可见节点头像 + 32 个阵营图标**下载到本地 `avatars/` 目录**（ASCII 安全命名），ECharts 内联绘制，彻底解决远程图异步加载导致的图谱头像空白问题
- 98 个无头像文件的 NPC 回退阵营色圆点；601 隐藏节点保留远程 URL 兜底（资料卡/时间线 `<img>` 用）

### 图谱视图
- 阵营标识：虚线边界圈 + PRTS 阵营图标 + 金色阵营名；circle packing 行星团布局，团间不重叠、等比缩放铺满画布
- 头像显示扩展至全量视图；阵营圈置底锁定，角色节点可拖拽
- 点选聚焦：直连边金色虚线 + 关系标签（md 原描述优先，长描述自动换行），邻居二级边关闭，无关边彻底隐藏
- 点击连线弹**关系浮窗**（档案关联 > 共同经历，右上角关闭按钮，头部可拖拽）+ 右侧详情面板 + 章节目录回溯
- Obsidian 风格动效：hover 放大发光、blur 聚焦淡化、选中节点金色呼吸光晕（zrender 直改，缩放/拖拽零延迟跟随）
- 阵营圈半径与间距优化，头像边缘钳制在圈内（布局与动画双层约束，不留越圈）

### 时间线视图
- 剧情详情卡片左侧新增「章节目录」，可跳转同段落任意幕

## 核心数据（v1.1）

- 角色：982 节点（129 重要），可见 381（601 非关键隐藏）
- 关系：10942 条边，305 条带 md 原描述
- 时间线：1113 幕完整剧情，主线按 EP00~EP17 编号
- 头像：283 角色头像 + 32 阵营图标全部本地化

## 文件清单（运行必需）

| 文件 | 说明 |
|---|---|
| `index.html` | 单页应用（图谱 + 时间线双视图），加载其余数据文件 |
| `graph.json` | 图谱数据：982 节点 / 10942 边 |
| `timeline.json` | 时间线：1113 幕 / 951 角色轨迹 |
| `scene_dialogues.json` | 每幕完整对话 + 叙事梗概 |
| `relation_details.json` | 关系详情：档案关联 + 共同经历 |
| `echarts.min.js` | ECharts 5.5.0 本地副本（离线可用） |
| `avatars.json` | 角色头像映射（本地 `avatars/` / 远程 CDN 兜底） |
| `faction_logos.json` | 阵营图标映射 |
| `avatars.zip` | 283 个角色头像 + 32 个阵营图标（本地图，解压后为 `avatars/` 目录，需与上述文件同目录） |

## 离线运行方法

```bash
# ① 解压头像包（仅首次）
unzip avatars.zip   # 解出 avatars/ 目录

# ② 启动本地服务
python -m http.server 8823   # 然后浏览器打开 http://127.0.0.1:8823
```

```bash
cd arknights-analysis-v1.1
python -m http.server 8823   # 然后浏览器打开 http://127.0.0.1:8823
```

直接双击打开 `index.html` 也可运行（无跨域限制的本机文件读取依赖低），但本地 http 服务体验最佳。

## 已知限制

- 全量视图仍有部分"未知"阵营（次要剧情角色，灰色）
- 个别规范点名角色 PRTS 无标准头像（阵营色块兜底）
